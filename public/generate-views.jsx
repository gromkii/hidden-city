function generatePreviews(){
  //create a new section

    //For each element in the resultsArray, generate a new previewObject div
      //Each div will use data.background image url

      //Also will contain a previewText h3 with data. name

  //After looping through, render the resultsArray, fade in if possible.

  
}

function generateBeerList(){
  //create a new section class lists

  //create a div with the brand logo

  //create a ul

    //for each beer listed in the brewery data, make a new li



  //After looping through, render the page, fade in blah blah.
}

function generateBeerInfo(){
  //create a new section class lists

  //create a div with label logo, if available

    //look through and determine what to lists
    //using object notation, go through and make lis for each key value pair
}
